import React from 'react';
import { Container, AppBar, Typography, Grow, Grid } from '@material-ui/core';

const App=() =>{
    return(
        <Container>
            <div>
            <h1>App</h1>
        </div>
        </Container>
    );
}

export default App;